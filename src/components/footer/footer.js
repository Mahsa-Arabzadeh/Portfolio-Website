"use strict";

import domGenerator from "dom-generator";

export default function contactSection() {
  return domGenerator({
    tag: "footer",
    attributes: { id: "footer" },
    children: [],
  });
}
